{{- if .Values.enabled -}}
{{- if .Values.proxy.enabled -}}
{{- if .Values.global.ingress.enabled | default .Values.ingress.enabled -}}
{{- $ingressPort := .Values.proxy.service.port -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Release.Name }}-fhir-proxy-ingress
  namespace: {{ .Release.Namespace }}
  {{- with .Values.fhir.proxy.labels }}
  labels:
    {{ toYaml . | nindent 4 }}
  {{- end }}
  annotations:
    kubernetes.io/ingress.class: {{ .Values.global.ingress.class | default .Values.ingress.class }}
spec:
  rules:
    - host: {{ .Release.Namespace }}-{{ .Release.Name }}-fhir-proxy.{{ .Values.global.ingress.subdomain | default .Values.ingress.subdomain }}
      http:
        paths:
          - path: /
            pathType: ImplementationSpecific
            backend:
              service:
                name: {{ .Release.Namespace }}-{{ .Release.Name }}-fhir
                port: 
                  number: {{ $ingressPort }}
{{- end }}
{{- end }}
{{- end }}