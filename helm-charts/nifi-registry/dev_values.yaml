# Default values for nifi-registry.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

ingress:
  enabled: false

image:
  repository: apache/nifi-registry
  pullPolicy: IfNotPresent
  tag: 0.8.0

service:
  type: ClusterIP
  port: 80

git:
  branch: origin
  username: atclark@us.ibm.com
  password: fec2f83f8a2ae508bb616f3ab67a248d085e7d52
  # By default this registry loads the health-patterns NiFi flows
  repository: https://github.ibm.com/integrationsquad/nifi-flows.git
  
resources: {}
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #   cpu: 100m
  #   memory: 128Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

podAnnotations: {}

podSecurityContext:
  fsGroup: 1001

securityContext:
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

nodeSelector: {}

tolerations: []

affinity: {}
