# Default values for cert-secret-generator.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

image:
  repository: quay.io/alvearie/cert-secret-generator
  pullPolicy: Always
  tag: 0.1.1

enabled: true
password: replace-me
