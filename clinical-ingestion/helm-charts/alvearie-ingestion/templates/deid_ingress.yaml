{{- if .Values.global.ingress.enabled -}}
{{- if index .Values "deid" "enabled" -}}
{{- $ingressPort := .Values.deid.service.port -}}
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: {{ .Release.Name }}-deid-ingress
  namespace: {{ .Release.Namespace }}
  {{- with .Values.deid.labels }}
  labels:
    {{ toYaml . | nindent 4 }}
  {{- end }}
  annotations:
    ingress.kubernetes.io/proxy-read-timeout: '90'
    kubernetes.io/ingress.class: public-iks-k8s-nginx
spec:
  rules:
    - host: {{ .Release.Namespace }}.{{ .Release.Name }}-deid.{{ .Values.global.ingress.subdomain}}
      http:
        paths:
          - path: /
            backend:
              serviceName: {{ .Release.Name }}-deid
              servicePort: {{ $ingressPort }}
{{- end }}
{{- end }}