{{- if .Values.global.ingress.enabled -}}
{{- if index .Values "fhir-deid" "enabled" -}}
{{- $ingressPort := index .Values "fhir-deid" "service" "httpPort" -}}
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: {{ .Release.Name }}-fhir-deid-ingress
  namespace: {{ .Release.Namespace }}
  {{- with index .Values "fhir-deid" "labels" }}
  labels:
    {{ toYaml . | nindent 4 }}
  {{- end }}
  annotations:
    ingress.kubernetes.io/proxy-read-timeout: '90'
    kubernetes.io/ingress.class: public-iks-k8s-nginx
spec:
  rules:
    - host: {{ .Release.Namespace }}.{{ .Release.Name }}-fhir-deid.{{ .Values.global.ingress.subdomain}}
      http:
        paths:
          - path: /
            backend:
              serviceName: {{ .Release.Name }}-fhir-deid
              servicePort: {{ $ingressPort }}
{{- end }}
{{- end }}